# Personal notes
#dev/*

# Claude Code local settings
.claude/

# Capacitor / Android build
node_modules/
package-lock.json
www/
apks/

# Native Android project is tracked (source only). Build artifacts and secrets
# are excluded by android/.gitignore (build/, .gradle/, *.aab/*.apk,
# local.properties, keystore.properties).
